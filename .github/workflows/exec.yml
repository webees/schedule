name: exec

on:
  workflow_dispatch:
    inputs:
      interval:
        description: "触发间隔 (1m, 5m, ...)"
        required: true

concurrency:
  group: exec-${{ inputs.interval }}
  cancel-in-progress: false

permissions:
  actions: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 业务执行
        run: |
          echo "exec | ${{ inputs.interval }} | $(date -u '+%Y-%m-%d %H:%M:%S') UTC | run#${{ github.run_number }}"

          if [ "${{ inputs.interval }}" = "1m" ]; then
            echo "--- 1分钟业务 ---"
            # TODO: 每分钟任务
          fi

          if [ "${{ inputs.interval }}" = "5m" ]; then
            echo "--- 5分钟业务 ---"
            # TODO: 每5分钟任务
          fi
