name: guard

on: workflow_dispatch

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

permissions:
  actions: write

jobs:
  guard:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
      REPO: ${{ github.repository }}
    steps:
      - id: check
        run: |
          alive=$(
            gh run list -R "$REPO" --limit 10 \
              --json workflowName,status \
              -q 'map(select(.status | test("progress|queued")))
                  | map(.workflowName)
                  | unique
                  | join(",")'
          )
          echo "alive=${alive}" >> "$GITHUB_OUTPUT"
      - run: |
          echo "$ALIVE" | grep -q tick-a || gh workflow run tick-a.yml -R "$REPO"
          echo "$ALIVE" | grep -q tick-b || gh workflow run tick-b.yml -R "$REPO"
        env:
          ALIVE: ${{ steps.check.outputs.alive }}
