name: tick-b

on:
  workflow_dispatch:

concurrency:
  group: tick-b
  cancel-in-progress: false

permissions:
  actions: write

jobs:
  tick:
    runs-on: ubuntu-latest
    steps:
      - name: ⏱️ tick-b
        run: echo "tick-b | $(date -u '+%Y-%m-%d %H:%M:%S') UTC | run#${{ github.run_number }}"

      - name: 💤 sleep 180s
        run: sleep 180

      - name: 🔄 自调度
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          QUEUED=$(gh run list -w tick-b.yml -s queued --json databaseId -q 'length' -R $REPO)
          [ "$QUEUED" -eq 0 ] && gh workflow run tick-b.yml -R $REPO

      - name: 👀 守护检测
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          for w in tick-a.yml tick-c.yml; do
            S=$(gh run list -w $w --json status -q '.[0].status' -R $REPO --limit 1)
            if [ "$S" != "in_progress" ] && [ "$S" != "queued" ]; then
              echo "⚠️ $w 已停止, 触发 revive"
              gh workflow run guard.yml -R $REPO 2>/dev/null || true
              break
            fi
          done
